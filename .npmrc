shamefully-hoist=true
# npm 10.9.x's arborist crashes ("Cannot read properties of null (reading 'edgesOut')")
# while resolving Nuxt 4's peer graph. This keeps `npm install` working locally and in CI.
legacy-peer-deps=true
